diff --git a/setup.py b/setup.py
new file mode 100644
index 0000000..81e2ccc
--- /dev/null
+++ b/setup.py
@@ -0,0 +1,62 @@
+# -*- coding: utf-8 -*-
+
+# DO NOT EDIT THIS FILE!
+# This file has been autogenerated by dephell <3
+# https://github.com/dephell/dephell
+
+try:
+    from setuptools import setup
+except ImportError:
+    from distutils.core import setup
+
+import os.path
+
+readme = ''
+here = os.path.abspath(os.path.dirname(__file__))
+readme_path = os.path.join(here, 'README.rst')
+if os.path.exists(readme_path):
+    with open(readme_path, 'rb') as stream:
+        readme = stream.read().decode('utf8')
+
+setup(
+    long_description=readme,
+    name='fastapi',
+    version='0.39.0',
+    description='FastAPI framework, high performance, easy to learn, fast to code, ready for production',
+    python_requires='>=3.6',
+    project_urls={
+        'homepage': 'https://github.com/tiangolo/fastapi',
+        'Documentation': 'https://fastapi.tiangolo.com/'
+    },
+    author='Sebastián Ramírez',
+    author_email='tiangolo@gmail.com',
+    classifiers=[
+        'License :: OSI Approved :: MIT License',
+        'Development Status :: 4 - Beta', 'Framework :: AsyncIO',
+        'Intended Audience :: Developers',
+        'Programming Language :: Python :: 3.6',
+        'Programming Language :: Python :: 3.7',
+        'Programming Language :: Python :: 3.8',
+        'Programming Language :: Python :: 3 :: Only',
+        'Topic :: Internet :: WWW/HTTP :: HTTP Servers'
+    ],
+    packages=[
+        'fastapi', 'fastapi.dependencies', 'fastapi.openapi', 'fastapi.security'
+    ],
+    package_data={'fastapi': ['*.typed']},
+    install_requires=[
+        'pydantic<=0.32.2,>=0.32.2', 'starlette<=0.12.8,>=0.11.1'
+    ],
+    extras_require={
+        'all': [
+            'aiofiles', 'email-validator', 'graphene', 'itsdangerous', 'jinja2',
+            'python-multipart', 'pyyaml', 'requests', 'ujson', 'uvicorn'
+        ],
+        'test': [
+            'black', 'databases[sqlite]', 'email-validator', 'isort', 'mypy',
+            'orjson', 'pytest>=4.0.0', 'pytest-cov', 'requests', 'sqlalchemy'
+        ],
+        'doc': ['markdown-include', 'mkdocs', 'mkdocs-material'],
+        'dev': ['passlib[bcrypt]', 'pyjwt']
+    },
+)
